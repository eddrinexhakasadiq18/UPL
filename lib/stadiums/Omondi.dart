import 'package:flutter/material.dart';

import '../Custom_app_Bar.dart';

class Omondi extends StatelessWidget {
  const Omondi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      appBar: CustomAppBar(titletext: "PHILLIP OMONDI STADIUM"),

      
    );
  }
}