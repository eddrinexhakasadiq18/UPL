import 'package:flutter/material.dart';

import '../Custom_app_Bar.dart';

class Kitende extends StatelessWidget {
  const Kitende({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      appBar: CustomAppBar(titletext: "ST MARY'S KITENDE"),

    
      
    );
  }
}