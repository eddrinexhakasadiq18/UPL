import 'package:flutter/material.dart';
import 'package:sportsapp/Custom_app_Bar.dart';



class BlackPowers extends StatelessWidget {
  const BlackPowers({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      appBar: CustomAppBar(titletext: "BLACK POWERS FOOTBALL CLUB"),

      
    );
  }
}