import 'package:flutter/material.dart';
import 'package:sportsapp/Custom_app_Bar.dart';



class Mbarara_city extends StatelessWidget {
  const Mbarara_city({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      appBar: CustomAppBar(titletext: "MBARARA CITY FOOTBALL CLUB"),

      
    );
  }
}